{
  "rules": {
    ".read": false,
    ".write": false,
    "chats": {
      "$chatId": {
        ".read": "auth != null && data.child('participants').child(auth.uid).exists()",
        ".write": "auth != null && ((!data.exists() && newData.child('participants').child(auth.uid).exists()) || (data.exists() && data.child('participants').child(auth.uid).exists()))",
        "messages": {
          "$messageId": {
            ".validate": "newData.hasChildren(['senderId','senderName','content','timestamp']) && newData.child('content').isString() && newData.child('content').val().length <= 4000 && (data.exists() ? (newData.child('senderId').val() == data.child('senderId').val() && newData.child('content').val() == data.child('content').val()) : newData.child('senderId').val() == auth.uid)"
          }
        }
      }
    }
  }
}